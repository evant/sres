apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile project(':sres-compile')
}

group = 'me.tatarka.sres.gradle'
version = '1.0-SNAPSHOT'

